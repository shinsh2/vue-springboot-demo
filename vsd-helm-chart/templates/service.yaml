apiVersion: v1
kind: Service
metadata:
  name: {{ include "vsd-helm-chart.fullname" . }}
  labels:
    {{- include "vsd-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vsd-helm-chart.selectorLabels" . | nindent 4 }}
